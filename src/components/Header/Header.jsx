import React from "react";
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <a href="./" className="logo-link">
                Notes app
            </a>
        </header>
    )
};

export default Header;